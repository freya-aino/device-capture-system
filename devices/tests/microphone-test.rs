#[cfg(test)]
mod microphone {

    #[test]
    fn open_mic() {
        // let mut mic = CpalMicrophoneDevice::new

        // let (data_tx, data_rx) = flume::bounded::<FramePacket>(32);

        // mic.initialize(&cpal_host).unwrap();

        // mic.start(MicrophoneConfig::new(16000, 1, 4), None, data_tx)
        //     .unwrap();

        // data_rx.into_iter().for_each(|frame| {
        //     println!("Received frame: {:?}", frame);
        // });
    }
}
